# helpers-lib.config — `C119`'s control: `helpers-default.config` plus one line. `./lib` REPLACES
# the defaults rather than joining them, so `uses-helpers.tflw` becomes the refused file here.

env local default
  api "http://localhost:9/v1"

helpers "./lib"
