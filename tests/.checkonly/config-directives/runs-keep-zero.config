# runs-keep-zero.config — tflw `M241` `E` (`D1325`): `C120`'s refusal. A page that keeps no run
# forgets every run it starts, so the count is at least 1.

runs keep 0

env local default
  api "http://localhost:9/v1"
