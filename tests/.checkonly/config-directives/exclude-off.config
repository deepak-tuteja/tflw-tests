# exclude-off.config — `M154g` step 4a: `C96`'s negative control. `exclude-on.config` minus its
# `exclude` line, so the count that differs between them is attributable to that line and to
# nothing else.

env local default
  api "http://localhost:4001/v1"
