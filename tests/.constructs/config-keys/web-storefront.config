# `C108`. The `web` base is the SPA storefront; `api` is declared because a config needs one and
# neither fixture makes an API call.

env local default
  api env TFLW_API_BASE default "http://localhost:4001/v1"
  web env TFLW_WEB_BASE default "http://localhost:8090"
