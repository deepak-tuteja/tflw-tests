# `C105`'s control. The 8443 listener's certificate is signed by a throwaway CA the container
# generates fresh at every start, so it cannot verify against any trust store — which is the whole
# situation the key exists for.

env local default
  api "https://localhost:8443/v1"
  allow hosts "localhost"
