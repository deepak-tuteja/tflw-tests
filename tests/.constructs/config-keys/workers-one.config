# `C101`. One file at a time (SPEC §12: the `workers` key is the default for `--parallel`, the file
# concurrency axis — not the `--workers` flag, which is the unrelated workload-generator axis).

defaults
  workers 1

env local default
  api "http://127.0.0.1:4507"
