# `C103`'s destination half — same threshold as `log-debug.config`, a different destination, so what
# moves between the two is where a `log` call goes and not whether it clears the level.

defaults
  log destination html
  log level debug

env local default
  api "http://127.0.0.1:4507"
