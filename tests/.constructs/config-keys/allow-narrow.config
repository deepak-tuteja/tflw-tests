# `C100`. The allowlist names the spelling the base URL uses and not the other one, so the absolute
# URL in `absolute.tflw` points at a host this config never declares.

env local default
  api "http://127.0.0.1:4507"
  allow hosts "127.0.0.1"
