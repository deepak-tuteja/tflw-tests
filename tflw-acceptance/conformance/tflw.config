# `M154b` — the conformance corpus: known-answer plants for tflw's own constructs, graded by
# `scripts/verify-construct-acceptance.mjs` against evidence the plants' targets record themselves.
#
# Its own config root rather than a directory under `perf/`, for two reasons. The first is
# discovery: `scripts/check-acceptance.mjs` walks every directory holding a `tflw.config`, so a new
# corpus is checked from the moment it has one — that script's own header says a hand-maintained
# list is exactly how the last corpus rotted. The second is the target: everything here points at
# `arrival-server.mjs`, a standalone counter with no database and no Docker stack, and pointing it
# at `perf/`'s config would silently make these plants depend on the dogfood stack being up.
env local default
  api "http://127.0.0.1:4507"
  # `M198` S1: `singletons.tflw` names an absolute `tflw://demoo/…` target to reach the runtime's
  # reserved-scheme refusal, and an absolute step URL under an env with no allowlist draws `TF058`.
  # Declaring one means the base's own host must be listed too (`TF036`, SPEC §3.7); nothing here
  # reaches anything else. `demoo` is never sent — the refusal fires before the allowlist is asked.
  allow hosts "127.0.0.1", "demoo"

# `M198` S2 — the one session this corpus declares, and it is declared here because a session can
# only be declared here (SPEC §3.3): `csrf from` is a property of a *credential*, so there is no
# position inside a `.tflw` file where the clause could be written. `sessions.tflw` is the plant
# that reads it back off the wire.
#
# The clause sits **after** the establishment step, not before it — `TF039` refuses a read of a
# response that has not happened yet, exactly as it refuses a premature `capture`.
session carrier
  api POST /session/issue
  expect status equals 200
  csrf from body.csrfToken send as header "X-CSRF-Token"
